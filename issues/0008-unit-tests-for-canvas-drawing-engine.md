## Parent

## What to build

Write unit tests for the `CanvasDrawingEngine` module. Tests should cover brush strokes, eraser functionality, and export logic.

## Acceptance criteria
- [ ] Test suite runs without errors.
- [ ] Test brush stroke creation with specific coordinates and color.
- [ ] Test eraser removes pixels within a defined radius.
- [ ] Test `exportToBase64()` correctly serializes canvas state to PNG base64 string.

## Blocked by
None - can start immediately